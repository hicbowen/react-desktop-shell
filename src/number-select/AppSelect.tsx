import { forwardRef } from 'react'; import type { AppSelectProps } from './types'; import './AppNumberSelect.css'
export const AppSelect = forwardRef<HTMLSelectElement, AppSelectProps>(function AppSelect({ className, defaultValue, invalid = false, onChange, onValueChange, options, placeholder, size = 'standard', value, ...rest }, ref) {
  const lookup = new Map(options.map((option) => [String(option.value), option.value]))
  return <span className={`app-select app-select--${size}${invalid ? ' app-select--invalid' : ''}${className ? ` ${className}` : ''}`}><select {...rest} aria-invalid={invalid || undefined} defaultValue={value === undefined ? defaultValue == null ? placeholder ? '' : undefined : String(defaultValue) : undefined} onChange={(event) => { onChange?.(event); onValueChange?.(lookup.get(event.target.value) ?? event.target.value) }} ref={ref} value={value === undefined ? undefined : String(value)}>{placeholder ? <option disabled value="">{placeholder}</option> : null}{options.map((option) => <option disabled={option.disabled} key={String(option.value)} value={String(option.value)}>{option.label}</option>)}</select><span aria-hidden="true" className="app-select__chevron">⌄</span></span>
})
