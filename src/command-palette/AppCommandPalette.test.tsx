// @vitest-environment jsdom
import { act } from 'react'; import { createRoot } from 'react-dom/client'; import { describe,expect,it,vi } from 'vitest'; import { AppCommandPalette } from './AppCommandPalette'
describe('AppCommandPalette',()=>{it('filters and executes commands with the keyboard',()=>{;(globalThis as {IS_REACT_ACT_ENVIRONMENT?:boolean}).IS_REACT_ACT_ENVIRONMENT=true;const host=document.createElement('div');const root=createRoot(host);const close=vi.fn();const execute=vi.fn();act(()=>root.render(<AppCommandPalette commands={[{id:'file.open',label:'Open file',execute},{id:'file.save',label:'Save file',execute:vi.fn()}]} onOpenChange={close} open/>));const input=document.body.querySelector<HTMLInputElement>('.app-command-palette__input')!;act(()=>{Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value')?.set?.call(input,'op');input.dispatchEvent(new Event('input',{bubbles:true}))});expect(document.body.querySelectorAll('[role="option"]')).toHaveLength(1);act(()=>input.dispatchEvent(new KeyboardEvent('keydown',{bubbles:true,key:'Enter'})));expect(execute).toHaveBeenCalledWith({commandId:'file.open',source:'palette'});expect(close).toHaveBeenCalledWith(false);act(()=>root.unmount())})})
