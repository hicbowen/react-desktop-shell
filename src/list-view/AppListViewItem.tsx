import type { AppListViewItemInternalProps } from './types'; import './AppListView.css'
export function AppListViewItem({ className, description, disabled = false, icon, interactive = true, itemRef, onItemClick, onItemKeyDown, secondaryText, selected = false, selectionMode = 'none', style, tabIndex = -1, title, trailing, value }: AppListViewItemInternalProps) {
  const classes = ['app-list-view-item', selected ? 'app-list-view-item--selected' : '', disabled ? 'app-list-view-item--disabled' : '', interactive ? 'app-list-view-item--interactive' : '', className].filter(Boolean).join(' ')
  return <div aria-disabled={disabled || undefined} aria-selected={selectionMode === 'none' ? undefined : selected} className={classes} data-value={value} onClick={(event) => onItemClick?.(value, event)} onKeyDown={(event) => onItemKeyDown?.(value, event)} ref={itemRef} role="option" style={style} tabIndex={disabled || !interactive ? -1 : tabIndex}>{selectionMode !== 'none' ? <span aria-hidden="true" className="app-list-view-item__selection">{selectionMode === 'multiple' ? selected ? '✓' : '' : ''}</span> : null}{icon ? <span aria-hidden="true" className="app-list-view-item__icon">{icon}</span> : null}<span className="app-list-view-item__content"><span className="app-list-view-item__top"><span className="app-list-view-item__title">{title}</span>{secondaryText ? <span className="app-list-view-item__secondary">{secondaryText}</span> : null}</span>{description ? <span className="app-list-view-item__description">{description}</span> : null}</span>{trailing ? <span className="app-list-view-item__trailing">{trailing}</span> : null}</div>
}
