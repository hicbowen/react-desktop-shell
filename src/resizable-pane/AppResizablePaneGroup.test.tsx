// @vitest-environment jsdom
import { act } from 'react'; import { createRoot } from 'react-dom/client'; import { describe, expect, it, vi } from 'vitest'; import { AppResizablePaneGroup } from './AppResizablePaneGroup'
describe('AppResizablePaneGroup', () => { it('supports bounded keyboard resizing', () => { const host=document.createElement('div'); const root=createRoot(host); const change=vi.fn(); act(()=>root.render(<AppResizablePaneGroup defaultSize={200} minSize={180} maxSize={220} onSizeChange={change}>{[<div key="a">A</div>,<div key="b">B</div>]}</AppResizablePaneGroup>)); const separator=host.querySelector<HTMLElement>('[role="separator"]')!; act(()=>separator.dispatchEvent(new KeyboardEvent('keydown',{bubbles:true,key:'End'}))); expect(separator.getAttribute('aria-valuenow')).toBe('220'); expect(change).toHaveBeenCalledWith(220); act(()=>root.unmount()) }) })
