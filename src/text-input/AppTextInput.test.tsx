// @vitest-environment jsdom
import { act, createRef } from 'react'; import { createRoot } from 'react-dom/client'; import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'; import { AppTextArea } from './AppTextArea'; import { AppTextBox } from './AppTextBox'
describe('text inputs', () => { let host: HTMLDivElement; let root: ReturnType<typeof createRoot>; beforeEach(() => { ;(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true; host = document.createElement('div'); document.body.append(host); root = createRoot(host) }); afterEach(() => { act(() => root.unmount()); host.remove() })
  it('forwards refs, changes, and clear actions', () => { const ref = createRef<HTMLInputElement>(); const onChange = vi.fn(); const onClear = vi.fn(); act(() => root.render(<AppTextBox clearable defaultValue="Ada" onChange={onChange} onClear={onClear} ref={ref} />)); expect(ref.current?.value).toBe('Ada'); act(() => host.querySelector('button')?.click()); expect(onClear).toHaveBeenCalledOnce(); expect(ref.current?.value).toBe('') })
  it('does not offer clear for passwords, disabled, or read-only values', () => { act(() => root.render(<><AppTextBox clearable defaultValue="secret" type="password" /><AppTextBox clearable defaultValue="x" disabled /><AppTextBox clearable defaultValue="x" readOnly /></>)); expect(host.querySelector('button')).toBeNull() })
  it('counts controlled and uncontrolled textarea content', () => { act(() => root.render(<><AppTextArea defaultValue="hello" maxLength={10} showCount /><AppTextArea showCount value="abc" readOnly /></>)); expect(host.textContent).toContain('5 / 10'); expect(host.textContent).toContain('3') })
  it('applies auto-resize height without a dependency', () => { Object.defineProperty(HTMLTextAreaElement.prototype, 'scrollHeight', { configurable: true, value: 80 }); act(() => root.render(<AppTextArea autoResize minRows={2} />)); expect(host.querySelector('textarea')?.style.height).toBe('80px') })
})
