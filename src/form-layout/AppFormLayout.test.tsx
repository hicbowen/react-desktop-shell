// @vitest-environment jsdom
import{act}from'react';import{createRoot,type Root}from'react-dom/client';import{afterEach,beforeEach,describe,expect,it,vi}from'vitest';import{AppFormLayout,AppValidationSummary}from'./AppFormLayout'
describe('form layout',()=>{let host:HTMLDivElement;let root:Root;beforeEach(()=>{host=document.createElement('div');document.body.append(host);root=createRoot(host)});afterEach(()=>{act(()=>root.unmount());host.remove()});it('sets horizontal label sizing',()=>{act(()=>root.render(<AppFormLayout labelWidth={180} orientation="horizontal"><span>Field</span></AppFormLayout>));expect((host.firstElementChild as HTMLElement).style.getPropertyValue('--app-form-label-width')).toBe('180px')});it('focuses fields from validation errors',()=>{const focus=vi.spyOn(HTMLInputElement.prototype,'focus');act(()=>root.render(<><input id="email"/><AppValidationSummary errors={[{key:'email',fieldId:'email',message:'Email is required'}]}/></>));act(()=>host.querySelector('button')!.click());expect(focus).toHaveBeenCalled();focus.mockRestore()})})
