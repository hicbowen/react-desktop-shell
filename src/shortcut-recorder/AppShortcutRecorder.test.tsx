// @vitest-environment jsdom
import{act}from'react';import{createRoot,type Root}from'react-dom/client';import{afterEach,beforeEach,describe,expect,it,vi}from'vitest';import{AppShortcutRecorder}from'./AppShortcutRecorder'
describe('AppShortcutRecorder',()=>{let host:HTMLDivElement;let root:Root;beforeEach(()=>{host=document.createElement('div');document.body.append(host);root=createRoot(host)});afterEach(()=>{act(()=>root.unmount());host.remove()});it('captures modified shortcuts',()=>{const change=vi.fn();act(()=>root.render(<AppShortcutRecorder onValueChange={change}/>));act(()=>host.querySelector('input')!.dispatchEvent(new KeyboardEvent('keydown',{bubbles:true,key:'k',ctrlKey:true,shiftKey:true})));expect(change).toHaveBeenCalledWith({key:'k',ctrl:true,shift:true,alt:undefined,meta:undefined})});it('reports conflicts',()=>{act(()=>root.render(<AppShortcutRecorder defaultValue={{key:'s',ctrl:true}} getConflict={()=> 'Already used'}/>));expect(host.querySelector('[role="alert"]')?.textContent).toBe('Already used')})})
