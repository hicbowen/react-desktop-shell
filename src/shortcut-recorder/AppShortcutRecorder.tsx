import{useState,type KeyboardEvent}from'react';import{formatAppShortcut,type AppShortcut}from'../command';import{useAppLocale}from'../localization/useAppLocale';import type{AppShortcutRecorderProps}from'./types';import'./AppShortcutRecorder.css'
const modifiers=new Set(['Alt','Control','Meta','Shift']);export function AppShortcutRecorder({value,defaultValue=null,onValueChange,getConflict,allowSingleKey=false,disabled=false,className,style}:AppShortcutRecorderProps){const{messages}=useAppLocale();const controlled=value!==undefined;const[internal,setInternal]=useState<AppShortcut|null>(defaultValue);const[recording,setRecording]=useState(false);const current=value===undefined?internal:value;const conflict=current?getConflict?.(current):null;const commit=(next:AppShortcut|null)=>{if(!controlled)setInternal(next);onValueChange?.(next)};const keyDown=(event:KeyboardEvent<HTMLInputElement>)=>{event.preventDefault();event.stopPropagation();if(event.key==='Escape'){setRecording(false);return}if(event.key==='Backspace'||event.key==='Delete'){commit(null);return}if(modifiers.has(event.key))return;if(!allowSingleKey&&!event.altKey&&!event.ctrlKey&&!event.metaKey&&!event.shiftKey)return;commit({key:event.key,alt:event.altKey||undefined,ctrl:event.ctrlKey||undefined,meta:event.metaKey||undefined,shift:event.shiftKey||undefined});setRecording(false)};return <span className={['app-shortcut-recorder',conflict?'app-shortcut-recorder--conflict':'',className].filter(Boolean).join(' ')} style={style}><input aria-label={messages.shortcutRecorder.label} disabled={disabled} onBlur={()=>setRecording(false)} onFocus={()=>setRecording(true)} onKeyDown={keyDown} placeholder={recording?messages.shortcutRecorder.recording:messages.shortcutRecorder.placeholder} readOnly value={current?formatAppShortcut(current):''}/>{current&&!disabled?<button aria-label={messages.shortcutRecorder.clear} onClick={()=>commit(null)} type="button">×</button>:null}{conflict?<span className="app-shortcut-recorder__conflict" role="alert">{conflict}</span>:null}</span>}
