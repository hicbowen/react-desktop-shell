import { useMemo,useState } from 'react'; import { FilePlus2,FolderOpen,Save } from 'lucide-react'; import { AppButton,AppCommandPalette,type AppCommand } from '../../../../src'; import { DemoPage,DemoSection } from '../../components/DemoPage'
export function AppCommandPalettePage(){const[open,setOpen]=useState(false);const commands=useMemo<AppCommand[]>(()=>[{id:'file.new',label:'New file',description:'Create an empty document',icon:<FilePlus2/>,shortcut:{ctrl:true,key:'n'},execute:()=>{}},{id:'file.open',label:'Open file',icon:<FolderOpen/>,shortcut:{ctrl:true,key:'o'},execute:()=>{}},{id:'file.save',label:'Save active file',icon:<Save/>,shortcut:{ctrl:true,key:'s'},execute:()=>{}}],[]);return <DemoPage><DemoSection title="Quick commands"><AppButton onClick={()=>setOpen(true)}>Open command palette</AppButton><AppCommandPalette commands={commands} onOpenChange={setOpen} open={open}/></DemoSection></DemoPage>}
